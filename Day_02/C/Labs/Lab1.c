//Including Libraries
#include <stdio.h>

// Declaration of enum
typedef enum { False, True } boolean;

void main(){
    //Printing Statements
    printf("False value in GCC = %d"
           "\nTrue value in GCC = %d", False, True);
}